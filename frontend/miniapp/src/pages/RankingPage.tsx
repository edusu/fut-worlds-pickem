/**
 * Renders the leaderboard for the user's pickem group.
 * Stub: placeholder until the ranking endpoint is implemented.
 */
export function RankingPage() {
  return (
    <main style={{ padding: 16 }}>
      <h1>Ranking</h1>
      <p>TODO: fetch /api/groups/&lt;id&gt;/ranking and render.</p>
    </main>
  );
}
