/// Handle `/new_pickem`. Creates a new pickem group bound to the current
/// Telegram chat, with the invoking user as the owner.
#[allow(dead_code)] // dispatcher wiring lands with the update loop
pub async fn handle() -> anyhow::Result<()> {
    todo!("commands::new_pickem::handle")
}
