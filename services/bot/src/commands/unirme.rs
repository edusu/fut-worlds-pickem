/// Handle `/unirme`. Adds the invoking user to the pickem group of the current
/// chat, idempotently.
#[allow(dead_code)] // dispatcher wiring lands with the update loop
pub async fn handle() -> anyhow::Result<()> {
    todo!("commands::unirme::handle")
}
